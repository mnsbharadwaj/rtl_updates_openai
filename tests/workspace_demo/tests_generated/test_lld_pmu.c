/* AUTO-GENERATED by lld_gen -- DO NOT EDIT */
/* Coverage: ALL fields | LLM=17 Template=0 */
#include <stdint.h>
#include <assert.h>
#include "sfr_pmu.h"
#include "lld_pmu.h"

#include <assert.h>

// Test function to set CLK_DIV register and verify it
static void test_clk_div_rw(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_DIV to a value (e.g., 3)
    uint8_t value = 3;
    lld_pmu_clk_con_clk_div_set(&lld, value);

    // Verify the CLK_DIV register
    assert(lld_pmu_clk_con_clk_div_get(&lld) == value);
}

// Test function to set CLK_DIV register and verify it (W1C)
static void test_clk_div_w1c(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_DIV to a value (e.g., 3)
    uint8_t value = 3;
    lld_pmu_clk_con_clk_div_set(&lld, value);

    // Verify the CLK_DIV register
    assert(lld_pmu_clk_con_clk_div_get(&lld) == value);
}

// Test function to set CLK_DIV register and verify it (W1S)
static void test_clk_div_w1s(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_DIV to a value (e.g., 3)
    uint8_t value = 3;
    lld_pmu_clk_con_clk_div_set(&lld, value);

    // Verify the CLK_DIV register
    assert(lld_pmu_clk_con_clk_div_get(&lld) == value);
}

// Test function to set CLK_DIV register and verify it (boundary values)
static void test_clk_div_boundary(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_DIV to the maximum value
    uint8_t max_value = 0xF;
    lld_pmu_clk_con_clk_div_set(&lld, max_value);

    // Verify the CLK_DIV register
    assert(lld_pmu_clk_con_clk_div_get(&lld) == max_value);
}

// Test function to set CLK_DIV register and verify it (boundary values)
static void test_clk_div_boundary_negative(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_DIV to the minimum value
    uint8_t min_value = 0x0;
    lld_pmu_clk_con_clk_div_set(&lld, min_value);

    // Verify the CLK_DIV register
    assert(lld_pmu_clk_con_clk_div_get(&lld

#include <assert.h>

// Test function to set CLK_EN and verify it
static void test_clk_en_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1; // Set the desired value for CLK_EN

    // Set CLK_EN using the setter function
    lld_pmu_clk_con_clk_en_set(&lld, value);

    // Verify that CLK_EN is set correctly by reading it back
    assert(lld_pmu_clk_con_clk_en_get(&lld) == value);
}

// Test function to clear CLK_EN and verify it
static void test_clk_en_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1; // Set the desired value for CLK_EN

    // Set CLK_EN using the setter function
    lld_pmu_clk_con_clk_en_set(&lld, value);

    // Clear CLK_EN using the clear function
    lld_pmu_clk_con_clk_en_clear(&lld);

    // Verify that CLK_EN is cleared correctly by reading it back
    assert(lld_pmu_clk_con_clk_en_get(&lld) == 0);
}

// Test function to set CLK_DIV and verify it
static void test_clk_div_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 2; // Set the desired value for CLK_DIV

    // Set CLK_DIV using the setter function
    lld_pmu_clk_con_clk_div_set(&lld, value);

    // Verify that CLK_DIV is set correctly by reading it back
    assert(lld_pmu_clk_con_clk_div_get(&lld) == value);
}

// Test function to clear CLK_DIV and verify it
static void test_clk_div_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 2; // Set the desired value for CLK_DIV

    // Set CLK_DIV using the setter function
    lld_pmu_clk_con_clk_div_set(&lld, value);

    // Clear CLK_DIV using the clear function
    lld_pmu_clk_con_clk_div_clear(&lld);

    // Verify that CLK_DIV is cleared correctly by reading it back
    assert(lld_pmu_clk_con_clk_div_get(&lld) == 0);
}

// Test function to set DMA_EN and verify it
static void test_dma_en_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pS

#include <assert.h>

static void test_clk_con_clk_gate_get(void)
{
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Inject 1U into the field
    sfr.stCLK_CON.stNative.CLK_GATE = 1U;

    // Assert that the getter returns 1U
    assert(lld_pmu_clk_con_clk_gate_get(&lld) == 1U);
}

static void test_clk_con_clk_gate_set(void)
{
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Call the setter with 1U
    lld_pmu_clk_con_clk_gate_set(&lld, 1U);

    // Assert that the field is now 1U
    assert(sfr.stCLK_CON.stNative.CLK_GATE == 1U);
}

static void test_clk_con_clk_gate_clear(void)
{
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Call the clear function
    lld_pmu_clk_con_clk_gate_clear(&lld);

    // Assert that the field is now 1U (since it was previously set to 1)
    assert(sfr.stCLK_CON.stNative.CLK_GATE == 1U);
}

static void test_clk_con_clk_gate_boundary(void)
{
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Inject the maximum value into the field
    sfr.stCLK_CON.stNative.CLK_GATE = 1U;

    // Assert that the getter returns the maximum value
    assert(lld_pmu_clk_con_clk_gate_get(&lld) == 1U);
}

int main(void)
{
    test_clk_con_clk_gate_get();
    test_clk_con_clk_gate_set();
    test_clk_con_clk_gate_clear();
    test_clk_con_clk_gate_boundary();

    return 0;
}

#include <assert.h>

// Test function to set CLK_SEL register and verify its value
void test_clk_sel_rw(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_SEL to 1 (PLL1)
    lld_pmu_clk_con_clk_sel_set(&lld, 1);

    // Verify that CLK_SEL is set to 1
    assert(lld_pmu_clk_con_clk_sel_get(&lld) == 1);
}

// Test function to clear CLK_SEL register and verify its value
void test_clk_sel_w1c(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_SEL to 1 (PLL1)
    lld_pmu_clk_con_clk_sel_set(&lld, 1);

    // Clear CLK_SEL
    lld_pmu_clk_con_clk_sel_clear(&lld);

    // Verify that CLK_SEL is cleared to 0
    assert(lld_pmu_clk_con_clk_sel_get(&lld) == 0);
}

// Test function to set CLK_SEL register and verify its value (RO)
void test_clk_sel_ro(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_SEL to 1 (PLL1)
    lld_pmu_clk_con_clk_sel_set(&lld, 1);

    // Read the value of CLK_SEL
    uint8_t clk_sel = lld_pmu_clk_con_clk_sel_get(&lld);

    // Verify that the read value is 1
    assert(clk_sel == 1);
}

// Test function to set CLK_DIV register and verify its value
void test_clk_div_rw(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_DIV to 3 (divide by 4)
    lld_pmu_clk_con_clk_div_set(&lld, 3);

    // Verify that CLK_DIV is set to 3
    assert(lld_pmu_clk_con_clk_div_get(&lld) == 3);
}

// Test function to clear CLK_DIV register and verify its value
void test_clk_div_w1c(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set CLK_DIV to 3 (divide by 4)
    lld_pmu_clk_con_clk_div_set(&lld, 3);

    // Clear CLK_DIV
    lld_pmu_clk_con_clk_div_clear(&lld);

    // Verify that CLK_DIV is

#include <assert.h>

// Define the PMU register structure
struct lld_PMU {
    struct {
        uint8_t IRQ_EN : 1;
    } stNative;
};

// Define the PMU SFR structure
struct SFR_PMU {
    struct lld_PMU *pSFR;
};

// Function to test the get accessor
static void test_get(void) {
    struct SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1U;

    // Inject the value into the field
    sfr.stIRQ_CON.stNative.IRQ_EN = value;

    // Assert that the getter returns the injected value
    assert(lld_PMU_irq_con_irq_en_get(&lld) == value);
}

// Function to test the set accessor (RW)
static void test_set(void) {
    struct SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1U;

    // Call the setter with the injected value
    lld_PMU_irq_con_irq_en_set(&lld, value);

    // Assert that the sfr field is equal to the injected value
    assert(sfr.stIRQ_CON.stNative.IRQ_EN == value);
}

// Function to test the clear accessor (W1C)
static void test_clear(void) {
    struct SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1U;

    // Call the clear function
    lld_PMU_irq_con_irq_en_clear(&lld);

    // Assert that the sfr field is equal to 1U after clearing
    assert(sfr.stIRQ_CON.stNative.IRQ_EN == 1U);
}

// Function to test the boundary value (max field value)
static void test_boundary(void) {
    struct SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 0xFF; // Max value for IRQ_EN

    // Inject the max value into the field
    sfr.stIRQ_CON.stNative.IRQ_EN = value;

    // Assert that the getter returns the injected value
    assert(lld_PMU_irq_con_irq_en_get(&lld) == value);
}

// Main function to run all tests
int main(void) {
    test_get();
    test_set();
    test_clear();
    test_boundary();

    return 0;
}

static void test_lld_pmu_irq_con_irq_en_irq_enable(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_en_irq_enable(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_EN == 1U);
}

static void test_lld_pmu_irq_con_irq_en_irq_disable(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    sfr.stIRQ_CON.stNative.IRQ_EN = 1U;
    lld_pmu_irq_con_irq_en_irq_disable(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_EN == 0U);
}

static void test_lld_pmu_irq_con_irq_en_irq_status(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    sfr.stIRQ_CON.stNative.IRQ_EN = 1U;
    assert(lld_pmu_irq_con_irq_en_irq_status(&lld) != 0U);
}

static void test_lld_pmu_irq_con_irq_en_irq_clear(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_en_irq_clear(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_EN == 1U);
}

#include <assert.h>

// Define the PMU register structure and its members
struct lld_PMU {
    struct {
        uint8_t IRQ_MASK : 2;
    } stNative;
};

// Define the PMU SFR structure and its members
struct SFR_PMU {
    struct lld_PMU *pSFR;
};

// Function to test the get operation
static void test_get(void) {
    struct SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1U;

    // Inject the value into the field
    sfr.stIRQ_CON.stNative.IRQ_MASK = value;

    // Assert that the getter returns the injected value
    assert(lld_PMU_irq_con_irq_mask_get(&lld) == value);
}

// Function to test the set operation (RW)
static void test_set(void) {
    struct SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1U;

    // Call the setter with the injected value
    lld_PMU_irq_con_irq_mask_set(&lld, value);

    // Assert that the sfr field is equal to the injected value
    assert(sfr.stIRQ_CON.stNative.IRQ_MASK == value);
}

// Function to test the clear operation (W1C)
static void test_clear(void) {
    struct SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1U;

    // Call the clear function
    lld_PMU_irq_con_irq_mask_clear(&lld);

    // Assert that the sfr field is equal to 1U after clearing
    assert(sfr.stIRQ_CON.stNative.IRQ_MASK == 1U);
}

// Function to test the boundary value (max field value)
static void test_boundary(void) {
    struct SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 3U;

    // Inject the max field value into the field
    sfr.stIRQ_CON.stNative.IRQ_MASK = value;

    // Assert that the getter returns the injected value
    assert(lld_PMU_irq_con_irq_mask_get(&lld) == value);
}

// Main function to run all tests
int main(void) {
    test_get();
    test_set();
    test_clear();
    test_boundary();

    return 0;
}

static void test_lld_pmu_irq_con_irq_mask_irq_enable(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_mask_irq_enable(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_MASK == 1U);
}

static void test_lld_pmu_irq_con_irq_mask_irq_disable(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    sfr.stIRQ_CON.stNative.IRQ_MASK = 1U;
    lld_pmu_irq_con_irq_mask_irq_disable(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_MASK == 0U);
}

static void test_lld_pmu_irq_con_irq_mask_irq_status(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    sfr.stIRQ_CON.stNative.IRQ_MASK = 1U;
    assert(lld_pmu_irq_con_irq_mask_irq_status(&lld) != 0U);
}

static void test_lld_pmu_irq_con_irq_mask_irq_clear(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_mask_irq_clear(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_MASK == 1U);
}

#include <assert.h>

// Define the PMU register structure and its members
typedef struct {
    uint8_t stNative[2]; // Assuming IRQ_CON is a 2-byte register
} SFR_PMU;

typedef struct {
    const SFR_PMU *pSFR;
} lld_PMU;

// Define the getter function for IRQ_PEND
uint8_t lld_pmu_irq_con_irq_pend_get(const lld_PMU *lld) {
    return lld->pSFR->stNative[0]; // Assuming IRQ_PEND is in the first byte of the register
}

// Define the setter function for IRQ_PEND
void lld_pmu_irq_con_irq_pend_set(lld_PMU *lld, uint8_t value) {
    lld->pSFR->stNative[0] = value; // Assuming IRQ_PEND is in the first byte of the register
}

// Define the clear function for IRQ_PEND
void lld_pmu_irq_con_irq_pend_clear(lld_PMU *lld) {
    lld->pSFR->stNative[0] &= ~1; // Clearing the least significant bit to 0
}

// Test function to inject a value into sfr field and assert getter returns the injected value
void test_set_rw(uint8_t value) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_pend_set(&lld, value);
    assert(lld_pmu_irq_con_irq_pend_get(&lld) == value);
}

// Test function to clear the IRQ_PEND field and assert it returns 1U
void test_clear_w1c() {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_pend_set(&lld, 1); // Set IRQ_PEND to 1
    lld_pmu_irq_con_irq_pend_clear(&lld);
    assert(lld_pmu_irq_con_irq_pend_get(&lld) == 1);
}

// Test function to inject the maximum value into sfr field and assert getter returns the maximum value
void test_max_value() {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_pend_set(&lld, 1); // Set IRQ_PEND to 1
    assert(lld_pmu_irq_con_irq_pend_get(&lld) == 1);
}

// Main function to run the tests
int main() {
    test_set_rw(1U);
    test_clear_w1c();
    test_max_value();
    return

static void test_lld_pmu_irq_con_irq_pend_irq_enable(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_pend_irq_enable(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_PEND == 1U);
}

static void test_lld_pmu_irq_con_irq_pend_irq_disable(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    sfr.stIRQ_CON.stNative.IRQ_PEND = 1U;
    lld_pmu_irq_con_irq_pend_irq_disable(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_PEND == 0U);
}

static void test_lld_pmu_irq_con_irq_pend_irq_status(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    sfr.stIRQ_CON.stNative.IRQ_PEND = 1U;
    assert(lld_pmu_irq_con_irq_pend_irq_status(&lld) != 0U);
}

static void test_lld_pmu_irq_con_irq_pend_irq_clear(void) {
    SFR_PMU sfr = {{0}};
    struct lld_pmu lld = { .pSFR = &sfr };
    lld_pmu_irq_con_irq_pend_irq_clear(&lld);
    assert(sfr.stIRQ_CON.stNative.IRQ_PEND == 1U);
}

#include <assert.h>

// Reference implementation not provided

static void test_pmu_ctrl_dma_en_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    sfr.stPMU_CTRL.stNative.DMA_EN = 1U;  // inject value
    assert(lld_PMU_pmu_ctrl_dma_en_get(&lld) == 1U);  // getter
}

static void test_pmu_ctrl_dma_en_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_PMU_pmu_ctrl_dma_en_set(&lld, 1U);  // set value
    assert(sfr.stPMU_CTRL.stNative.DMA_EN == 1U);  // setter
}

static void test_pmu_ctrl_dma_en_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_PMU_pmu_ctrl_dma_en_clear(&lld);  // clear value
    assert(sfr.stPMU_CTRL.stNative.DMA_EN == 1U);  // clear
}

static void test_pmu_ctrl_dma_en_boundary(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    sfr.stPMU_CTRL.stNative.DMA_EN = 0x1;  // inject max value
    assert(lld_PMU_pmu_ctrl_dma_en_get(&lld) == 0x1);  // getter
}

int main(void) {
    test_pmu_ctrl_dma_en_get();
    test_pmu_ctrl_dma_en_set();
    test_pmu_ctrl_dma_en_clear();
    test_pmu_ctrl_dma_en_boundary();

    return 0;
}

#include <assert.h>

// Reference implementation not available

static void test_pmu_ctrl_dma_pass_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Inject 1U into the field
    sfr.stPMU_CTRL.stNative.DMA_PASS = 1U;

    // Assert that the getter returns 1U
    assert(lld_PMU_pmu_ctrl_dma_pass_get(&lld) == 1U);
}

static void test_pmu_ctrl_dma_pass_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Call the setter with 1U
    lld_PMU_pmu_ctrl_dma_pass_set(&lld, 1U);

    // Assert that the field in sfr is now 1U
    assert(sfr.stPMU_CTRL.stNative.DMA_PASS == 1U);
}

static void test_pmu_ctrl_dma_pass_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Call the clear function
    lld_PMU_pmu_ctrl_dma_pass_clear(&lld);

    // Assert that the field in sfr is now 1U (since it was cleared)
    assert(sfr.stPMU_CTRL.stNative.DMA_PASS == 1U);
}

static void test_pmu_ctrl_dma_pass_boundary(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Inject the maximum value into the field
    sfr.stPMU_CTRL.stNative.DMA_PASS = 1U;

    // Assert that the getter returns the maximum value
    assert(lld_PMU_pmu_ctrl_dma_pass_get(&lld) == 1U);
}

int main(void) {
    test_pmu_ctrl_dma_pass_get();
    test_pmu_ctrl_dma_pass_set();
    test_pmu_ctrl_dma_pass_clear();
    test_pmu_ctrl_dma_pass_boundary();

    return 0;
}

#include <assert.h>

// Define the PMU_CTRL register structure
struct lld_PMU {
    struct {
        uint8_t CPU_RST : 1;
    } stNative;
};

// Define the PMU_CTRL register access functions
uint8_t lld_PMU_pmu_ctrl_hcpu_rst_get(const struct lld_PMU *lld) {
    return lld->stNative.CPU_RST;
}

void lld_PMU_pmu_ctrl_hcpu_rst_set(struct lld_PMU *lld, uint8_t value) {
    lld->stNative.CPU_RST = value;
}

// Define the PMU_CTRL register clear function
void lld_PMU_pmu_ctrl_hcpu_rst_clear(struct lld_PMU *lld) {
    lld->stNative.CPU_RST = 0;
}

// Test function to inject a value into the CPU_RST field and assert the getter returns the injected value
static void test_set_rw(uint8_t value) {
    struct lld_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_PMU_pmu_ctrl_hcpu_rst_set(&lld, value);
    assert(lld_PMU_pmu_ctrl_hcpu_rst_get(&lld) == value);
}

// Test function to clear the CPU_RST field and assert it returns 0
static void test_clear_w1c() {
    struct lld_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_PMU_pmu_ctrl_hcpu_rst_clear(&lld);
    assert(lld_PMU_pmu_ctrl_hcpu_rst_get(&lld) == 0);
}

// Test function to inject the maximum value into the CPU_RST field and assert the getter returns the maximum value
static void test_max_value() {
    struct lld_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_PMU_pmu_ctrl_hcpu_rst_set(&lld, 1);
    assert(lld_PMU_pmu_ctrl_hcpu_rst_get(&lld) == 1);
}

// Main function to run all tests
int main() {
    test_set_rw(1);  // Test RW access pattern
    test_clear_w1c();  // Test W1C access pattern
    test_max_value();  // Test boundary value

    return 0;
}

#include <assert.h>

// Reference implementation not available

static void test_pmu_ctrl_hcpu_vinit_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    sfr.stPMU_CTRL.stNative.HCPU_VINIT = 1U;  // inject value
    assert(lld_pmu_pmu_ctrl_hcpu_vinit_get(&lld) == 1U);  // getter
}

static void test_pmu_ctrl_hcpu_vinit_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_pmu_pmu_ctrl_hcpu_vinit_set(&lld, 1U);  // set value
    assert(sfr.stPMU_CTRL.stNative.HCPU_VINIT == 1U);  // setter
}

static void test_pmu_ctrl_hcpu_vinit_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_pmu_pmu_ctrl_hcpu_vinit_clear(&lld);  // clear value
    assert(sfr.stPMU_CTRL.stNative.HCPU_VINIT == 1U);  // clear
}

static void test_pmu_ctrl_hcpu_vinit_max(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    sfr.stPMU_CTRL.stNative.HCPU_VINIT = 1U;  // inject max value
    assert(lld_pmu_pmu_ctrl_hcpu_vinit_get(&lld) == 1U);  // getter
}

int main(void) {
    test_pmu_ctrl_hcpu_vinit_get();
    test_pmu_ctrl_hcpu_vinit_set();
    test_pmu_ctrl_hcpu_vinit_clear();
    test_pmu_ctrl_hcpu_vinit_max();

    return 0;
}

#include <stdint.h>

// Define the SFR structure and LLD structure
typedef struct {
    uint32_t stRST_CON;
} SFR_PMU;

typedef struct {
    SFR_PMU *pSFR;
} lld_PMU;

// Define the test functions
static void test_rst_con_ret_con_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint32_t value = 0x12345678; // Example value
    sfr.stRST_CON.stNative.RET_CON = value;
    assert(lld_PMU_rst_con_ret_con_get(&lld) == value);
}

static void test_rst_con_ret_con_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint32_t value = 0x12345678; // Example value
    lld_PMU_rst_con_ret_con_set(&lld, value);
    assert(sfr.stRST_CON.stNative.RET_CON == value);
}

static void test_status_con_done_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1; // Example value
    sfr.stSTATUS_CON.stNative.COMPLETE = value;
    assert(lld_pmu_status_con_done_get(&lld) == value);
}

static void test_status_con_done_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1; // Example value
    lld_pmu_status_con_done_set(&lld, value);
    assert(sfr.stSTATUS_CON.stNative.COMPLETE == 0); /* W1C: write 1 to clear */
}

static void test_status_con_err_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1; // Example value
    sfr.stSTATUS_CON.stNative.ERR = value;
    assert(lld_pmu_status_con_err_get(&lld) == value);
}

static void test_status_con_level_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    uint8_t value = 1; // Example value
    sfr.stSTATUS_CON.stNative.LEVEL = value;
    assert(lld_pmu_status_con_level_get(&lld) == value

#include <assert.h>

// Test function for lld_pmu_status_con_abort_get
static void test_lld_pmu_status_con_abort_get(void)
{
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    // Inject 1U into the field
    sfr.stSTATUS_CON.stNative.ABORT = 1U;
    // Assert that the getter returns 1U
    assert(lld_pmu_status_con_abort_get(&lld) == 1U);
}

// Test function for lld_pmu_status_con_abort_set (RW)
static void test_lld_pmu_status_con_abort_set(void)
{
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    // Call the setter with 1U
    lld_pmu_status_con_abort_set(&lld, 1U);
    // Assert that the field in sfr is now 1U
    assert(sfr.stSTATUS_CON.stNative.ABORT == 1U);
}

// Test function for lld_pmu_status_con_abort_clear (W1C)
static void test_lld_pmu_status_con_abort_clear(void)
{
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    // Call the clear function
    lld_pmu_status_con_abort_clear(&lld);
    // Assert that the field in sfr is now 1U (assuming it was previously set)
    assert(sfr.stSTATUS_CON.stNative.ABORT == 1U);
}

// Test function for boundary value (max field value)
static void test_lld_pmu_status_con_abort_max(void)
{
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    // Inject the maximum value into the field
    sfr.stSTATUS_CON.stNative.ABORT = 1U;
    // Assert that the getter returns the maximum value
    assert(lld_pmu_status_con_abort_get(&lld) == 1U);
}

// Main function to run all tests
int main(void)
{
    test_lld_pmu_status_con_abort_get();
    test_lld_pmu_status_con_abort_set();
    test_lld_pmu_status_con_abort_clear();
    test_lld_pmu_status_con_abort_max();
    return 0;
}

#include <assert.h>

// Reference implementation not available

static void test_pmu_status_con_complete_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    sfr.stSTATUS_CON.stNative.COMPLETE = 1U;  // inject value
    assert(lld_PMU_status_con_complete_get(&lld) == 1U);  // getter
}

static void test_pmu_status_con_complete_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_PMU_status_con_complete_set(&lld, 1U);  // set value
    assert(sfr.stSTATUS_CON.stNative.COMPLETE == 1U);  // setter
}

static void test_pmu_status_con_complete_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    lld_PMU_status_con_complete_clear(&lld);  // clear value
    assert(sfr.stSTATUS_CON.stNative.COMPLETE == 1U);  // clear
}

static void test_pmu_status_con_complete_boundary(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };
    sfr.stSTATUS_CON.stNative.COMPLETE = 0x1;  // inject max value
    assert(lld_PMU_status_con_complete_get(&lld) == 0x1);  // getter
}

int main(void) {
    test_pmu_status_con_complete_get();
    test_pmu_status_con_complete_set();
    test_pmu_status_con_complete_clear();
    test_pmu_status_con_complete_boundary();

    return 0;
}

#include <assert.h>

// Test function to set and get the ERR field in STATUS_CON register
void test_status_con_err_set_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the ERR field to 1
    lld_PMU_status_con_err_set(&lld, 1);

    // Assert that the ERR field in STATUS_CON register is set to 1
    assert(lld_PMU_status_con_err_get(&lld) == 1);
}

#include <assert.h>

// Test function to set and get the LEVEL field of STATUS_CON register
void test_level_rw(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the LEVEL field to a value (e.g., 1)
    lld_pmu_status_con_level_set(&lld, 1);

    // Assert that the LEVEL field is set correctly
    assert(lld_pmu_status_con_level_get(&lld) == 1);
}

// Test function to clear the DMA_DONE flag of STATUS_CON register
void test_dma_done_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the DMA_DONE flag
    lld_pmu_status_con_done_set(&lld);

    // Clear the DMA_DONE flag
    lld_pmu_status_con_done_clear(&lld);

    // Assert that the DMA_DONE flag is cleared correctly
    assert(lld_pmu_status_con_done_get(&lld) == 0);
}

// Test function to set the LEVEL field of STATUS_CON register with a boundary value (max)
void test_level_max(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the LEVEL field to its maximum value
    lld_pmu_status_con_level_set(&lld, 3);

    // Assert that the LEVEL field is set correctly
    assert(lld_pmu_status_con_level_get(&lld) == 3);
}

// Test function to clear the DMA_DONE flag of STATUS_CON register with a boundary value (max)
void test_dma_done_max(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the DMA_DONE flag
    lld_pmu_status_con_done_set(&lld);

    // Clear the DMA_DONE flag
    lld_pmu_status_con_done_clear(&lld);

    // Assert that the DMA_DONE flag is cleared correctly
    assert(lld_pmu_status_con_done_get(&lld) == 0);
}

#include <assert.h>

// Test function to set and get the THRESH field in STATUS_CON register
static void test_status_con_thresh_set_get(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the THRESH field to a value (e.g., 15)
    sfr.stSTATUS_CON.stNative.THRESH = 15;

    // Assert that the THRESH field in STATUS_CON register matches the set value
    assert(lld_PMU_status_con_thresh_get(&lld) == 15);
}

// Test function to clear the DMA_DONE flag in STATUS_CON register
static void test_status_con_dma_done_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the DMA_DONE flag (e.g., 1)
    lld_PMU_status_con_done_set(&lld);

    // Assert that the DMA_DONE flag in STATUS_CON register is cleared
    assert(lld_PMU_status_con_done_get(&lld) == 0);
}

// Test function to set the THRESH field in STATUS_CON register
static void test_status_con_thresh_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the THRESH field to a value (e.g., 15)
    sfr.stSTATUS_CON.stNative.THRESH = 15;

    // Assert that the THRESH field in STATUS_CON register matches the set value
    assert(lld_PMU_status_con_thresh_get(&lld) == 15);
}

// Test function to clear the DMA_DONE flag in STATUS_CON register
static void test_status_con_dma_done_clear(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the DMA_DONE flag (e.g., 1)
    lld_PMU_status_con_done_set(&lld);

    // Assert that the DMA_DONE flag in STATUS_CON register is cleared
    assert(lld_PMU_status_con_done_get(&lld) == 0);
}

// Test function to set the THRESH field in STATUS_CON register
static void test_status_con_thresh_set(void) {
    SFR_PMU sfr = {0};
    struct lld_PMU lld = { .pSFR = &sfr };

    // Set the THRESH field to a value (e.g., 15)
    sfr.stSTATUS_CON.stNative.THRESH = 15;

    // Assert that the THRESH field in STATUS_CON register matches the set value
    assert(lld_PMU_status_con_thresh_get(&

int main(void) {
    test_clk_div_rw();
    test_clk_div_w1c();
    test_clk_div_w1s();
    test_clk_div_boundary();
    test_clk_div_boundary_negative();
    test_clk_en_set();
    test_clk_en_clear();
    test_clk_div_set();
    test_clk_div_clear();
    test_dma_en_set();
    test_clk_con_clk_gate_get();
    test_clk_con_clk_gate_set();
    test_clk_con_clk_gate_clear();
    test_clk_con_clk_gate_boundary();
    test_get();
    test_set();
    test_clear();
    test_boundary();
    test_lld_pmu_irq_con_irq_en_irq_enable();
    test_lld_pmu_irq_con_irq_en_irq_disable();
    test_lld_pmu_irq_con_irq_en_irq_status();
    test_lld_pmu_irq_con_irq_en_irq_clear();
    test_get();
    test_set();
    test_clear();
    test_boundary();
    test_lld_pmu_irq_con_irq_mask_irq_enable();
    test_lld_pmu_irq_con_irq_mask_irq_disable();
    test_lld_pmu_irq_con_irq_mask_irq_status();
    test_lld_pmu_irq_con_irq_mask_irq_clear();
    test_lld_pmu_irq_con_irq_pend_irq_enable();
    test_lld_pmu_irq_con_irq_pend_irq_disable();
    test_lld_pmu_irq_con_irq_pend_irq_status();
    test_lld_pmu_irq_con_irq_pend_irq_clear();
    test_pmu_ctrl_dma_en_get();
    test_pmu_ctrl_dma_en_set();
    test_pmu_ctrl_dma_en_clear();
    test_pmu_ctrl_dma_en_boundary();
    test_pmu_ctrl_dma_pass_get();
    test_pmu_ctrl_dma_pass_set();
    test_pmu_ctrl_dma_pass_clear();
    test_pmu_ctrl_dma_pass_boundary();
    test_pmu_ctrl_hcpu_vinit_get();
    test_pmu_ctrl_hcpu_vinit_set();
    test_pmu_ctrl_hcpu_vinit_clear();
    test_pmu_ctrl_hcpu_vinit_max();
    test_rst_con_ret_con_get();
    test_rst_con_ret_con_set();
    test_status_con_done_get();
    test_status_con_done_clear();
    test_status_con_err_get();
    test_status_con_level_get();
    test_lld_pmu_status_con_abort_get();
    test_lld_pmu_status_con_abort_set();
    test_lld_pmu_status_con_abort_clear();
    test_lld_pmu_status_con_abort_max();
    test_pmu_status_con_complete_get();
    test_pmu_status_con_complete_set();
    test_pmu_status_con_complete_clear();
    test_pmu_status_con_complete_boundary();
    test_status_con_thresh_set_get();
    test_status_con_dma_done_clear();
    test_status_con_thresh_set();
    test_status_con_dma_done_clear();
    test_status_con_thresh_set();
    return 0;
}
